import 'package:flutter/material.dart';

class DualListBox extends StatelessWidget {
  const DualListBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
